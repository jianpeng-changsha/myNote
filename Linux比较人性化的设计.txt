cp复制选项，文件之间是不能进行赋值的，但可以考虑将文件复制到文件夹中，语法是cp -r stage1 stage2/.
find查找命令：匹配任意多个任意字符使用的是find . -name "file*"
系统调用就是内核的接口，应用程序可以通过系统调用来使用内核提供的功能，Unix操作系统提供了shell来实现和用户之间的，shell是一个命令行解释器，它读取用户输入，然后执行命令。shell拥有很多版本
类Unix系统之间的联系：为了减少开发成本提升代码的可移植性，于是提出了POSIX标准，POSIX标准为各个系统调用和系统库的接口提供规范（IEEE 1003.1），只需要进行少量甚至无需修改就可以在不同的Unix发行的版本的机器上使用
普通用户与特权用户：普通用户的shell命令提示符是$root的shell命令提示符是#，每个用户都在一个组当中，组被用于将若干用户集合到项目或者部门当中去，组机制允许同组的各个成员可以共享资源，为了节约空间，操作在保存用户和组信息的时候只保存两个整形数字，成为用户ID和组ID，用户ID和组ID和用户名和组名都是一一对应的
Linux自带目录的含义：/bin 存放二进制文件，通常是一些可执行程序；/sys用于存放系统文件；/dev存放的是设备文件；/etc存放的是配置文件和启动脚本；/home放的是普通用户家目录的父目录；/lib存放的是系统的库文件；/proc特殊的目录里面的内容是对文件系统进行的映射；/usr里面存普通用户可以访问的文件，包括库文件，二进制文件；/var文件存储变化很快的文件比如日志；

Linuxfind指令中的奇妙操作：find 目录 标记 名字：-name查找文件名匹配的所给字符串的所有文件，字符串奶可以使用通配符*、？、[]；-gid查找ID号为n的用户组的所有文件；-uid查找属于ID号为你的用户的所有文件；-group查找属于用户名为所给的字符串的所有的文件；-user 查找属于用户名为所给字串的所有文件-empty 查找大小为0的目录或文件。
-perm 权限 查找具有指定权限的文件和目录，权限的表示可以如711，644-size n[bckw] 查找指定文件大小的文件，n后面的字符表示单位，缺省为b，代表512字节的块-type x 查找类型为x的文件根据时间查找amin n 查找n分钟以前被访问过的所有文件（+表示n分钟之前，-表示n分钟之内，+号和-号都不能省略）cmin n 查找n分钟以前文件状态被修改过的所有文件mmin n 查找n分钟以前文件内容被修改过的所有文件atime n 查找n天以前被访问过的所有文件ctime n 查找n天以前文件状态被修改过的所有文件mtime n 查找n天以前文件内容被修改过的所有文件其他的查找条件可以在man帮助里面找到

查看文件系统的整体磁盘空间的使用情况：df -h 显示整个磁盘的使用情况；2.显示每隔文件和目录的磁盘使用空间：du [选项] [文件名] 

useradd -m表示自动建立用户的登入目录；-s表示指定用户登入后所使用的的shell

#tail -f 文件路径：该命令一般用于查看系统的日志比较多。

wc 用于统计文件中的信息：默认情况下会显示文件中的行数，单词数和字节数；
clear并没有清除之前的信息而是将之前的信息隐藏到了上面；

管道：管道用于过滤特殊和扩展处理，需要配合前面的指令来进行辅助作用，以管道作为分界线，前面的命令有个输出，后面需要先输入，然后再过滤，最后再输出，通俗的讲就是管道前面的输出就是后面指令的输入；

ps -ef指令:-e：等价于“-A”，表示列出全部的进程-f：显示全部的列（显示全字段）
列的含义：
UID：该进程执行的用户id；
PID：进程id；
PPID：该进程的父级进程id，如果一个程序的父级进程找不到，该程序的进程称之为僵尸进程（parent process ID）；
C：Cpu的占用率，其形式是百分数；
STIME：进行的启动时间；
TTY：终端设备，发起该进程的设备识别符号，如果显示“?”则表示该进程并不是由终端设备发起；
TIME：进程的执行时间；
CMD：该进程的名称或者对应的路径；


top指令
PID：进程id；
USER：该进程对应的用户；
PR：优先级；
VIRT：虚拟内存；
RES：常驻内存；
SHR：共享内存；
	计算一个进程实际使用的内存 = 常驻内存（RES）- 共享内存（SHR）
S：表示进程的状态status（sleeping，其中S表示睡眠，R表示运行）；
%CPU：表示CPU的占用百分比；
%MEM：表示内存的占用百分比；
TIME+：执行的时间；
COMMAND：进程的名称或者路径；
service指令:用于控制一些软件的服务启动/停止/重启 #service 服务名 start/stop/restart

reboot:reboot 重启 reboot -w模拟重启，但是不重启（只写关机开机的日志信息）
shutdown：关机；shutdown -hnow 关机提示
其他的关机命令：init0 halt poweroff
如何在命令行中快速删除光标前/后的内容？   前：ctrl + u   后：ctrl + k

vim编辑器下的常规操作：
输入：“:!外部命令” 先去执行外部命令，当执行完外部命令之后按任意键就可以回到vim编辑器打开的内容；

vim的个人配置文件和系统的全局配置文件之间给听谁的：如果针对同一个配置项，个人配置文件中存在，则以个人配置文件为准，如果个人配置文件中不存在这一项，则以全局配置文件为准。
init进程对应一个配置文件：inittab；文件中的级别：0表示关机；1表示单用户模式；2表示多用户模式，不带NFS（Network file system）；3多用户模式，完全不带多用户模式不带桌面的纯命令模式；4 没有被使用的模式（被保留模式）5Xll表示完整的图形化界面模式；6表示重启的级别；

用户和用户组的管理：

三个重要的文件：/etc/passwd存储用户的关键信息；/etc/group 存储用户的关键信息；/etc/shadow存储用户的密码信息
Linux系统开发比较好用的几个终端工具：xshell，securecrt、putty  ssh 服务文件传输：可以采用可视化的传输工具filezilla；

Linux网络编程的相关指令：
netstat：#netstat -tnlp:-t表示使用tcp协议，-n表示将字幕转换为数字，-l列出所有的状态为监听状态-p显式进程相关的信息；
	#netstat -an（-a表示全部，-n可以将字母转换为数字）表示的是正在活跃的Internet网络的连接，也就是说当前所有连接到本地机器的所有连接状态：从左向右依次表示的是：协议的类型接收队列发送队列本地地址外部地址和状态；
traceroute：查找当前主机与目标主机之间所有的网关（路由器，会给沿途的路由器发送ICMP数据包，路由器可能不给相应）
ARP：根据ip地址来获取mac物理地址的协议：当一个主机发送数据是首先检查马超地址缓存中有没有目标地址的MAC地址，如果有就是用缓存中的结果；如果没有；ARP协议就会发出一个广播包，该广播包要求查询目标主机ip地址对应的马超地址，这样发送方就得到了目标主机的MAC地址，如果目标主机不在本地子网中，则ARP解析得到的MAC地址是默认网关的MAC地址；


shell脚本编程： 什么是脚本：简单的说 就是一条条的命令文字，这些命令文字是可以看到的（如可以用记事本打开查看编辑，）常见的脚本主要有js,vbscript,asp,jsp,php后端，sql，Perl ，shell，py,ruby,javafx,lua
shell不懂得省略空格；
shell的数学计算问题：原生的bash并不支持简单的数学运算，但是可以通过指令来实现，例如AWK和expr最为常用，ecpr是一款表达式计算工具使用它能后完成表达式的求职操作，但还是有两点需要注意的地方：表达式与运算符之间要有空格，完整的表达式要被`    `包围
shell中人性化的逻辑运算符：-eq检测两个数是否相等；-gt检测左边的数是否大于右边的数，如果是则返回TRUE；-lt检测左边的数是否小于右边的，如果是则返回TRUE；-ge检测左边的数是否大于厚泽等于右边的如果是则返回TRUE；le检测左边的数是够小于右边的，吐过是则返回TRUE；
-o表示或者运算符，-a表示与运算符
字符串运算符：-z检测字符串的长度是否是0 如果为0 则返回TRUE；-n检测字符串的长度是否为0如果不为0 则返回TRUE；str检测字符串是否为空，不为空则返回TRUE
文件测试运算符：-b file 检测文件是否是块设备文件,-c file检测文件是否是字符设备文件，如果是则返回TRUE，-d file检测是否是目录，如果是则返回TRUE，-f file检测文件时候是普通文件如果是返回TRUE

安装软件时用到的yum和rpm
mysql:
mysql的使用经验：首先要操作数据库必须要选择一个数据库，或者是创建一个数据库
MySQL键的使用经验：
插入数据外键关联的住键必须要存在，不能删除一个被外间关联的数据，要想删除次序先删除外键；
有外键关联的表，使用create table name select * from hero；只能复制表中的数据不能复制表的结构，如果采用create table name like name；只能复制表的结构但是不能复制表中的数据；
MySQL的备份与还原：全量备份：#mysqlump -uroot -p123456 -A>备份文件路径；指定数据库进行备份：#mysqlunp -uroot -p123456 库名>备份文件路径；多个库备份：mysqlump -uroot -p23456 --databases db1 db2>备份文件路径；
还原数据库可以使用sourse方法或者是采用系统命令行方法，1.还原全部的数据库：mysql>source 备份文件路径2.采用系统命令行：mysql -uroot -p123456<备份文件路径；还原单个数据库

云项目上线实战：
